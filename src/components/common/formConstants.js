const types = {
  NUMBER: 'number',
  TEXT: 'text',
  CHECKBOX: 'checkbox',
  SELECT: 'select',
  EMAIL: 'email',
};

const currencyPlaceholder = '##CURR##';
const percentSign = String.fromCharCode(37);

module.exports = {
  types,
  currencyPlaceholder,
  percentSign,
};
