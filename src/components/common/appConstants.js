const formTypes = {
  NUMBER: 'number',
  TEXT: 'text',
  CHECKBOX: 'checkbox',
  SELECT: 'select',
  EMAIL: 'email',
};

const currencyPlaceholder = '##CURR##';
const toastPlaceHolder = '##LABEL##';
const percentSign = String.fromCharCode(37);

module.exports = {
  formTypes,
  currencyPlaceholder,
  percentSign,
  toastPlaceHolder,
};
